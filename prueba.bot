create 
    int bot bar
        on activation:
            store 3.
        end
    end
    int bot contador
        on default:
            store 35.
        end
    end
execute
    activate contador.
    $- CALLATE
         activate -$
    activate blaaak.
    if bar > 2:
        advance contador.
    end
end
