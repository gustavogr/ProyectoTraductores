create 
    int bot bar
        on activation:
            store 3.
        end
    end
    int bot contador
        on default:
            store 35.
        end
    end
execute
    deactivate contador, bar. $$, bar esto da peos en la accion agregar
    $- CALLATE
         activate -$
    deactivate blaaak.
    if 1 <= 3:
        advance contador, blah, quepaso, acevedo.
    $$else:
    $$    deactivate aa.
    end

end
